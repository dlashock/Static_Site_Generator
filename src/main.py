from textnode import TextNode, TextType
from htmlnode import HTMLNode, LeafNode, ParentNode

def main():
    pass
    # test = "**this** is a *line* of *text*"
    # test_split = test.split("**")
    # print(test_split)
    # print(len(test_split) % 2 == 0)

main()